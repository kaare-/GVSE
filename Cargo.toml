[workspace]
resolver = "2"
members = [
    "crates/wk-material",
    # Active voxel stack.
    "crates/wk-voxel",
    "crates/wk-voxel-app",
    # Legacy column stack (reference only — not the development path).
    # See docs/VOXEL_MIGRATION.md.
    "crates/legacy/wk-field",
    "crates/legacy/wk-world",
    "crates/legacy/wk-agents",
    "crates/legacy/wk-sim",
    "crates/legacy/wk-io",
    "crates/legacy/wk-app",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

# Shared third-party pins — bump here, not per-crate.
[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
postcard = { version = "1", features = ["alloc"] }
# 0.11 needs rustc > 1.83 for `query_disjoint_mut`.
hecs = "=0.10.5"
# 0.4.15 uses const f32::{min,max,abs} that need rustc > 1.83.
macroquad = "=0.4.14"
rayon = "1.10"

[profile.test]
opt-level = 2
